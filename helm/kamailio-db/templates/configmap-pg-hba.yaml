apiVersion: v1
kind: ConfigMap
metadata:
  name: kamailio-db-pg-config
data:
  pg_hba.conf: |
    # TYPE  DATABASE        USER            ADDRESS                 METHOD
    local   all             all                                     trust
    host    all             all             0.0.0.0/0               md5